import"./NZTpNUN0.js";import"./BkvxQF8z.js";import{t as h,c as o,f as c,d as s,S as n,B as i,P as p,q as g}from"./D3cAVrQp.js";const u={title:"Contributing to the Website"},{title:y}=u;var m=h('<h2>What this codebase is</h2> <p>The site is a <a href="https://kit.svelte.dev" rel="nofollow">SvelteKit</a> application that publishes several dozen interlinked governance frameworks in three languages. Most of the complexity isn’t in the UI — it’s in how content, metadata, navigation, and translations stay in sync with each other.</p> <p><strong>The stack:</strong></p> <ul><li><strong>SvelteKit</strong> — routing, SSR, the whole application</li> <li><strong>mdsvex</strong> — markdown processing for long-form framework documents</li> <li><strong>Custom CSS</strong> — no Tailwind, no utility framework. Component-scoped styles with CSS custom properties for the palette.</li> <li><strong>Custom i18n</strong> — a small store-based system in <code>src/lib/i18n/</code>, with JSON translation files per locale</li> <li><strong>Custom schema</strong> — <code>src/lib/schema/_index.ts</code> is the single source of truth for framework metadata</li></ul> <p>Two of those are worth flagging because they contradict what you might reasonably assume. There is no Tailwind here, and older documentation that mentions it is out of date. And the schema is authoritative: titles, tiers, related frameworks, and publication state all derive from it, so if you find yourself maintaining a parallel list of frameworks somewhere, that’s a bug rather than a pattern.</p> <h2>Getting set up</h2> <pre class="language-bash"><!></pre> <p>That should bring the site up on <code>http://localhost:5173</code>. Node 18 or higher.</p> <p>If the dev server complains about schema validation, read the output before assuming you broke something — <code>validate-schema.js</code> distinguishes blocking errors from hygiene warnings, and the hygiene ones are pre-existing.</p> <h2>Where things live</h2> <pre class="language-undefined"><!></pre> <p>Framework pages are largely generated rather than hand-written. A dynamic <code>[slug]</code> route handles both outline mode and prose mode, so adding a framework usually means adding a schema entity and content files, not creating a route folder.</p> <h2>Conventions worth knowing before your first PR</h2> <p><strong>Styling.</strong> Write component-scoped CSS, using the existing custom properties (<code>--primary-blue</code>, <code>--warm-gold</code>, <code>--content-text</code> and so on) rather than hardcoded hex values. Don’t introduce utility classes.</p> <p><strong>Text.</strong> User-visible strings go in the i18n JSON files, not inline in components. Every new string needs English, Swedish, and Japanese. If you can only do one, add English and say so in the PR — a missing translation is a known state the system handles, but a hardcoded English string in a component is invisible breakage for two thirds of the audience.</p> <p><strong>Swedish capitalization.</strong> Swedish doesn’t use title case. Capitalize the first word of a heading and proper nouns only.</p> <p><strong>Markdown gotcha.</strong> In mdsvex, a bare <code>&lt;</code> immediately before a digit is parsed as the start of a tag and will fail compilation. Wrap it in a backtick code span. HTML entities won’t work, because remark decodes them before the Svelte compiler runs.</p> <h2>Good places to start</h2> <p>The most useful contributions right now are unglamorous: accessibility fixes, responsive layout problems, dead code, and pages where the copy has drifted out of date relative to what the project actually is. There is a lot of the last category.</p> <p>If you want something more substantial, open an issue describing what you have in mind before building it. The architecture has a few opinions baked into it, and it’s easier to talk through the approach first than to unpick a large PR afterwards.</p> <h2>Submitting work</h2> <p>Fork, branch, and open a pull request against <code>main</code>. Keep pull requests focused — one concern per PR reviews much faster than a mixed bag.</p> <p>Reviews come from one person, so response times vary. If something has been sitting for a while, a nudge on Discord is welcome rather than annoying.</p> <h2>Getting help</h2> <p>Discord is the fastest route for questions, and it’s small enough that you’ll usually get an answer from whoever wrote the code you’re asking about. GitHub issues work too, and are better for anything worth recording.</p>',1);function v(r){var t=m(),e=o(c(t),12),l=s(e);n(l,()=>`<code class="language-bash"><span class="token function">git</span> clone https://github.com/GlobalGovernanceFrameworks/website.git
<span class="token builtin class-name">cd</span> website
<span class="token function">npm</span> <span class="token function">install</span>
<span class="token function">npm</span> run dev -- <span class="token parameter variable">--open</span></code>`),i(e);var a=o(e,8),d=s(a);n(d,()=>`<code class="language-undefined">src/
├── lib/
│   ├── components/     Reusable Svelte components
│   ├── content/        Markdown content, organised by locale
│   ├── i18n/           Translation system + en/ sv/ ja/ JSON files
│   ├── schema/         _index.ts — authoritative framework metadata
│   └── utils/
└── routes/             SvelteKit pages</code>`),i(a),p(28),g(r,t)}export{v as default,u as metadata};
